export function TinyGpsMapCard() {
  return (
    <div className="panel backprop-card">
      <h3>Map of Language</h3>
      <p className="panel-subtle">
        The full demo stays in-repo. See <span className="backprop-mono">docs/phase3/map_of_language.md</span> for
        setup and exploration notes.
      </p>
      <p className="diag-note">
        The frontend view will hook into the embeddings once the demo scripts are wired.
      </p>
    </div>
  );
}
